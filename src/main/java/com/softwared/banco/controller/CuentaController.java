package com.softwared.banco.controller;

import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequestMapping("api/cuenta")
public class CuentaController {
	/*
	 * @Autowired private CuentaService cuentaService;
	 * 
	 * @PostMapping(path = "/crearCuenta", consumes =
	 * MediaType.APPLICATION_JSON_VALUE) public ResponseEntity<AuthResponse>
	 * crearCuenta(@RequestBody Cuenta cuenta) throws SistemaBancarioExcepcion {
	 * return ResponseEntity.ok(
	 * cuentaService.createCuenta(cuenta.getNumeroCuenta(),
	 * cuenta.getNombreTitular(), cuenta.getPasswordTitular(),
	 * cuenta.getSaldoInicial())); }
	 */
}
